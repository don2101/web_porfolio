<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- xml mapper와 짝이 될 java mapper의 패키지명(경로명)과 파일명을 적어줌 -->
<mapper namespace="com.ssafy.mapper.PortfolioMapper">

	

	<select id="getPostList" resultType="Post">
		select * from post order by postId
	</select>
	
	<select id="getDetailPost" resultType="Post" parameterType="String">
		select * from post where postId=#{param1}
	</select>
  
 	<insert id="insertPostInfo" parameterType="Post">
 		insert into post values(null,#{pfImg},#{title},sysdate(),#{content},0)
 	</insert>
 	
 	<delete id="deletePostList" parameterType="String">
 		delete from post where pfId = #{param1}
 	</delete>
 	
 	<update id="updatePortfolioInfo">
 		update portfolio 
 		set pfImg =#{portfolio.pfImg},
 		title = #{portfolio.title},  
 		dateq = sysdate(), 
 		content =#{portfolio.content} 
 		where pfId =#{pfId}
 	</update>
 	

  	
</mapper>








